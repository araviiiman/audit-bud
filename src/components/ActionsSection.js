import React from 'react';

const ActionsSection = () => {
  // This component is now empty since screenshot button moved to sidebar
  // Keeping it for potential future actions
  return null;
};

export default ActionsSection;
